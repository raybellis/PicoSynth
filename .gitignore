build/
.cache/

# editor configuration, generated by utils/clangd_config.py.  it names
# one toolchain at one version, so it is only right for the machine
# that wrote it
.clangd

# left behind by running the generators in utils/
__pycache__/
test.mid
test2.mid
test-aftertouch.mid
test-gm.mid
test-stress.mid
test-stress2.mid
